#include<stdio.h>
int main()
{
    int result,answer;
    printf("Enter any negative number = ");
    scanf("%d",&result);
    answer=abs(result);
    printf("The Answer is = %d",answer);
    return 0;
}
